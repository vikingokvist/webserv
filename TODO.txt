TODO limits

- IF in .conf file, there is a variable with no value set (example: root;) it should throw an error - CONFIGPARSER

- IF netcat and you leave connection open, the server shouldnt hang indefenitely, and close the connection

- check in request if non-ascii characters


- Checkar que todo tire errores correctos a std::cerr

CONFIG file

- CHeck for errors if location missing path name

- variable space semicolon doesnt throw error (methods ;)

 - Multiple variables inside location should throw error

 - in the same server, even though having multiple listen directives is allowed, if on the same port, should throw error
  (different servers with same port are allowed)

- 